apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd

resources:
  - projects.yaml
  # Wave 0: Essential components
  - cilium-app.yaml
  - coredns-app.yaml
  # Wave 1: Sealed secrets
  - sealed-secrets-app.yaml
  # Wave 2: Storage components
  - longhorn-app.yaml
  # Wave 3: ArgoCD and Gateways
  - argocd-app.yaml
  - cert-manager-app.yaml
  - traefik-app.yaml
  # Wave 4: Infrastructure
  - external-dns-app.yaml
  # Wave 5: Monitoring
  - prometheus-app.yaml
  - grafana-app.yaml